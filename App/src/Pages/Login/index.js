import React from 'react'
import HeaderLogin from '../../components/HeaderLogin'

const Login = ({action}) => {
  return (
    <div className="Login">
      <HeaderLogin action={action} />
    
    </div>
  )
}

export default Login
